@{
    ViewData["Title"] = "Home";
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <div class="text-center mt-5">
        <h1 class="display-4">¡Bienvenido a Mi Proyecto!</h1>
        <p class="lead">Aquí no solo aprenderás sobre ASP.NET Core, ¡sino que también podrás divertirte!</p>
        <p>Recuerda: <strong>Programar es como un chiste:</strong> si tienes que explicarlo, ¡no es tan gracioso!</p>
        
        <a href="@Url.Action("Index", "Auth")" class="btn btn-success btn-lg mt-4">¡Vamos a Iniciar Sesión!</a>
        
        <div class="mt-5">
            <button id="countButton" class="btn btn-info btn-lg">Haz clic aquí para contar!</button>
            <p id="clickCount" class="mt-3">Número de clics: <span id="count">0</span></p>
            <p id="royaltyMessage" class="mt-3 text-success" style="display:none;">¡Eres el rey de los clics!</p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        let count = 0;
        document.getElementById('countButton').addEventListener('click', function() {
            count++;
            document.getElementById('count').textContent = count;

            if (Math.random() < 0.2) {
                document.getElementById('royaltyMessage').style.display = 'block';
                setTimeout(function() {
                    document.getElementById('royaltyMessage').style.display = 'none';
                }, 2000);
            }
        });
    </script>
</body>
</html>
